fun main(args: Array<String>) {
    val input = readLine()!!.split(" ").map { it.toInt() }
    val a = input[0] * input[2]
    val gcd = gcd(a, input[1])
    
    print("${a / gcd} ${input[1] / gcd}")
}

fun gcd(a: Int, b: Int): Int {
    return if (b == 0) a else gcd(b, a % b)
}