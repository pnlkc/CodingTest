fun main() {
    val sb = StringBuilder()
    val T = readln().toInt()

    for (tc in 1..T) {
        val (A, B) = readln().split(" ").map { it.toInt() }

        sb.appendLine("yes")
    }
    
    print(sb)
}